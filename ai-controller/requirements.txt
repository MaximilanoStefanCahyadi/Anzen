# ============================================================
# Smart Door Lock - Requirements
# Install semua dependensi dengan:
#   pip install -r requirements.txt
#
# Catatan sistem (install via apt, BUKAN pip):
#   sudo apt install libzbar0          # pyzbar (QR scanner)
#   sudo apt install libopenblas-dev   # numpy performance di Raspberry Pi
#   sudo apt install cmake             # dibutuhkan saat build dlib
#   sudo apt install libatlas-base-dev # dibutuhkan dlib di Raspberry Pi
# ============================================================

# --- Computer Vision ---
opencv-python==4.10.0.84        # cv2: kamera, render, resize frame

# --- Face Recognition ---
face-recognition==1.3.0         # face_recognition: deteksi & encoding wajah
dlib==19.24.6                   # dependensi wajib face-recognition
numpy==1.26.4                   # array encoding wajah & operasi matrix

# --- QR Code Scanner ---
pyzbar==0.1.9                   # decode QR code dari frame kamera
# WAJIB: sudo apt install libzbar0

# --- Firebase ---
firebase-admin==6.5.0           # Realtime Database: logs, queue, tokens, health

# --- GPIO (Raspberry Pi only) ---
gpiozero==2.0.1                 # kontrol relay solenoid via GPIO pin
RPi.GPIO==0.7.1                 # backend GPIO untuk gpiozero di Raspberry Pi
